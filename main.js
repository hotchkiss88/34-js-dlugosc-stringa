let first = "Uwielbiam JavaScript";

let second = "Jestem świetnym programistą";

function smush(x, y) {

    let result = x + y;
    return result
};


let effect = smush(first, second);
console.log(effect);